export type IEvent = {
  id: string;
  title: string;
  description: string;
  price: string;
  totalSeats?: string;
  imageUrl: string;
  eventDate: string;
  createdAt: string;
  updatedAt: string;
};
